(() => {
  'use strict';
  const cfg=window.AGCH_CONFIG||{};
  const db=window.supabase?.createClient?.(cfg.supabaseUrl,cfg.supabaseKey,{auth:{persistSession:true,autoRefreshToken:true}});
  if(!db)return;
  if(!document.getElementById('middleDrawAdminStyles')){const st=document.createElement('style');st.id='middleDrawAdminStyles';st.textContent=`#middleDrawAdminRoot{display:grid;gap:16px}.mad-card{background:#fff;border:1px solid #e2e8f0;border-radius:20px;padding:18px;box-shadow:0 8px 28px rgba(20,40,60,.06)}.mad-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.mad-grid>div,.mad-team{padding:12px;border-radius:14px;background:#f7fafc;border:1px solid #e6edf4}.mad-grid small,.mad-team small{display:block;color:#7c8798;font-size:11px}.mad-grid b,.mad-team b{display:block;margin-top:3px;color:#172435}.mad-grid code{font-size:10px;word-break:break-all}.mad-state{display:inline-flex!important;width:max-content;padding:5px 10px;border-radius:999px}.mad-state.pending{background:#fff6d9;color:#8b6500}.mad-state.live{background:#e7f8ee;color:#08733e}.mad-state.completed{background:#edf2f7;color:#334155}.mad-teams{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:10px 0 16px}.mad-schedule{display:flex;gap:10px;align-items:end}.mad-schedule label{flex:1;font-weight:700;color:#334155}.mad-schedule input{display:block;width:100%;box-sizing:border-box;margin-top:7px;border:1px solid #d7e0e9;border-radius:12px;padding:11px;background:#fff}.mad-warning{margin:16px 0 10px;padding:13px;border-radius:14px;background:#fff8e6;border:1px solid #f0d995;color:#765500;font-size:13px}.mad-start{width:100%;min-height:48px;font-weight:900}.mad-result{margin-top:16px;padding:15px;border-radius:16px;background:#f4faf7;border:1px solid #cde9da}.mad-result code{display:block;margin-top:8px;font-size:10px;word-break:break-all;color:#75610d}.mad-audit{display:grid;gap:8px}.mad-audit>div{padding:11px;border-bottom:1px solid #edf1f5}.mad-audit b,.mad-audit span,.mad-audit small{display:block}.mad-audit span{margin:3px 0;color:#526071;font-size:12px}.mad-audit small{color:#8c97a5;font-size:10px}@media(max-width:800px){.mad-grid{grid-template-columns:1fr 1fr}.mad-teams{grid-template-columns:1fr}.mad-schedule{display:grid}.mad-card{padding:14px}}`;document.head.appendChild(st)}
  const MIDDLE='4b420e85-19b3-479c-bd79-e0fef79a105f';
  let draw=null,teams=[],audit=[];
  const esc=(v='')=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const team=id=>teams.find(t=>t.id===id);
  function addUi(){if(document.getElementById('drawAdmin'))return;const nav=document.querySelector('.tabs');const refsBtn=nav?.querySelector('[data-tab="refs"]');const btn=document.createElement('button');btn.dataset.tab='drawAdmin';btn.innerHTML='<i>◉</i> إدارة القرعة';refsBtn?.after(btn);const sec=document.createElement('section');sec.id='drawAdmin';sec.className='section';sec.innerHTML='<div class="head"><div><span class="admin-kicker">LIVE DRAW</span><h2>إدارة القرعة</h2></div></div><div id="middleDrawAdminRoot"></div>';document.querySelector('.admin-app')?.appendChild(sec);btn.addEventListener('click',()=>{document.querySelectorAll('.section').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));sec.classList.add('active');btn.classList.add('active');document.querySelector('.admin-sidebar')?.classList.remove('open');render()})}
  async function load(){const [{data:d},{data:t},{data:a}]=await Promise.all([db.from('middle_round_three_draws').select('*').eq('tournament_id',MIDDLE).eq('stage','الدور الثالث').maybeSingle(),db.from('teams').select('id,name,logo_url').eq('tournament_id',MIDDLE),db.from('audit_logs').select('id,action,summary,changed_by,created_at').eq('table_name','middle_round_three_draws').order('created_at',{ascending:false}).limit(20)]);draw=d;teams=t||[];audit=a||[];render()}
  function statusLabel(){if(!draw)return'غير متاحة';if(draw.status==='completed')return'انتهت';if(draw.status==='live')return'مباشرة';return'لم تبدأ'}
  function participants(){return (draw?.participant_team_ids||[]).map(id=>`<div class="mad-team"><b>${esc(team(id)?.name||'فريق')}</b><small>${esc(id.slice(0,8))}</small></div>`).join('')}
  function localValue(iso){if(!iso)return'';const d=new Date(iso);return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,16)}
  function render(){const root=document.getElementById('middleDrawAdminRoot');if(!root||!draw)return;const locked=draw.status!=='pending';root.innerHTML=`<div class="mad-card"><div class="mad-grid"><div><small>البطولة</small><b>بطولة الوسط</b></div><div><small>المرحلة</small><b>الدور الثالث</b></div><div><small>الحالة</small><b class="mad-state ${esc(draw.status)}">${statusLabel()}</b></div><div><small>معرف القرعة</small><code>${esc(draw.id)}</code></div></div><h3>الفرق المشاركة</h3><div class="mad-teams">${participants()}</div><div class="mad-schedule"><label>موعد القرعة<input id="madDate" type="datetime-local" ${locked?'disabled':''} value="${localValue(draw.scheduled_at)}"></label><button id="madSave" class="ghost" ${locked?'disabled':''}>حفظ الموعد</button></div>${draw.status==='pending'?`<div class="mad-warning">⚠️ سيتم إجراء القرعة رسميًا وحفظ النتيجة، ولن يكون بالإمكان إعادة اختيار الفرق بعد بدء العملية.</div><button id="madStart" class="primary mad-start">بدء القرعة</button>`:`<div class="mad-result"><h3>نتيجة القرعة</h3><p>نصف النهائي: <b>${esc(team(draw.semifinal_team_a_id)?.name||'—')} × ${esc(team(draw.semifinal_team_b_id)?.name||'—')}</b></p><p>المتأهل مباشرة: <b>${esc(team(draw.direct_finalist_team_id)?.name||'—')}</b></p><code>${esc(draw.verification_hash||'')}</code></div>`}</div><div class="mad-card"><h3>سجل القرعة</h3><div class="mad-audit">${audit.length?audit.map(x=>`<div><b>${esc(x.action)}</b><span>${esc(x.summary||'')}</span><small>${esc(x.changed_by||'')} · ${new Date(x.created_at).toLocaleString('ar-MR')}</small></div>`).join(''):'<p>لا توجد عمليات بعد.</p>'}</div></div>`;document.getElementById('madSave')?.addEventListener('click',saveSchedule);document.getElementById('madStart')?.addEventListener('click',startDraw)}
  async function saveSchedule(){const input=document.getElementById('madDate');if(!input?.value)return alert('حدد تاريخ ووقت القرعة أولًا.');const {error}=await db.rpc('set_middle_round_three_draw_schedule',{p_scheduled_at:new Date(input.value).toISOString()});if(error)return alert('تعذر حفظ الموعد: '+error.message);await load()}
  async function startDraw(){if(!confirm('سيتم إجراء القرعة رسميًا الآن وحفظ النتيجة نهائيًا. لا يمكن إعادة اختيار الفرق بعد البدء. هل تريد المتابعة؟'))return;const b=document.getElementById('madStart');if(b){b.disabled=true;b.textContent='جارٍ بدء القرعة…'}const {error}=await db.rpc('start_middle_round_three_draw');if(error){alert('لم تبدأ القرعة: '+error.message);if(b){b.disabled=false;b.textContent='بدء القرعة'}return}await load();setTimeout(load,10000)}
  addUi();load();db.channel('middle-draw-admin-live').on('postgres_changes',{event:'*',schema:'public',table:'middle_round_three_draws'},()=>load()).subscribe();
})();