(()=>{'use strict';const FALLBACK='assets/logo-placeholder.svg';const safe=fn=>{try{return fn()}catch(e){console.error(e);return null}};function fixImages(root=document){root.querySelectorAll('img').forEach(img=>{if(img.dataset.stable==='1')return;img.dataset.stable='1';img.decoding='async';if(!img.closest('.brand'))img.loading='lazy';img.addEventListener('error',()=>{if(!img.src.endsWith(FALLBACK))img.src=FALLBACK},{once:true})})}function removeDeadLinks(root=document){root.querySelectorAll('a[href="#"],a[href=""],button[disabled][data-page]').forEach(el=>{if(el.matches('a'))el.removeAttribute('href')})}function keepActiveNav(){const active=document.querySelector('.page.active')?.id;if(!active)return;document.querySelectorAll('.nav [data-page],.desktop-nav [data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===active))}function ensureTapTargets(root=document){root.querySelectorAll('button,a,[role="button"]').forEach(el=>{if(!el.style.minHeight)el.style.minHeight='44px'})}function fixOverflow(root=document){root.querySelectorAll('.match-card,.teamcard,.news-card,.action-card,.player-row,.search-result').forEach(el=>{el.style.minWidth='0';el.style.maxWidth='100%'})}function heal(){safe(()=>fixImages());safe(()=>removeDeadLinks());safe(()=>ensureTapTargets());safe(()=>fixOverflow());safe(()=>keepActiveNav())}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',heal,{once:true});else heal();let queued=false;new MutationObserver(()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;heal()})}).observe(document.documentElement,{subtree:true,childList:true});window.addEventListener('pageshow',heal);window.addEventListener('online',()=>{document.documentElement.classList.remove('offline')});window.addEventListener('offline',()=>{document.documentElement.classList.add('offline')});window.addEventListener('unhandledrejection',e=>console.error('Unhandled promise rejection',e.reason));})();