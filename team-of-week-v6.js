(()=>{'use strict';const cfg=window.AGCH_CONFIG||{};const main=document.getElementById('appMain');if(!main||!cfg.supabaseUrl||!cfg.supabaseKey)return;const H={apikey:cfg.supabaseKey,Authorization:`Bearer ${cfg.supabaseKey}`,'Content-Type':'application/json'};const esc=(v='')=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const q=async p=>{const r=await fetch(`${cfg.supabaseUrl}/rest/v1/${p}`,{headers:H,cache:'no-store'});if(!r.ok)throw new Error(`${p}:${r.status}`);return r.json()};const rpc=async(fn,b={})=>{const r=await fetch(`${cfg.supabaseUrl}/rest/v1/rpc/${fn}`,{method:'POST',headers:H,body:JSON.stringify(b),cache:'no-store'});if(!r.ok)throw new Error(await r.text());return r.json()};const token=()=>localStorage.getItem('agh_prediction_session')||'';const setToken=v=>v&&localStorage.setItem('agh_prediction_session',v);let S={tournaments:[],rounds:[],round:null,players:[],teams:[],slots:{},formation:'4-3-3',saved:false,stats:[],official:[],crowd:[]},sheet=null,loading=false;const formations={'4-3-3':[['هجوم',['fwd1','fwd2','fwd3']],['وسط',['mid1','mid2','mid3']],['دفاع',['def1','def2','def3','def4']],['حارس مرمى',['gk']]],'4-4-2':[['هجوم',['fwd1','fwd2']],['وسط',['mid1','mid2','mid3','mid4']],['دفاع',['def1','def2','def3','def4']],['حارس مرمى',['gk']]],'3-5-2':[['هجوم',['fwd1','fwd2']],['وسط',['mid1','mid2','mid3','mid4','mid5']],['دفاع',['def1','def2','def3']],['حارس مرمى',['gk']]]};const slotGroup=k=>k==='gk'?'حارس مرمى':k.startsWith('def')?'دفاع':k.startsWith('mid')?'وسط':'هجوم';const route=()=>location.hash.replace(/^#\/?/,'').split('/').filter(Boolean);const isTow=()=>route()[0]==='team-of-week';const tournamentFromRoute=()=>route()[1]||null;function player(id){return S.players.find(x=>x.player_id===id)||{}}function team(id){return S.teams.find(x=>x.id===id)||{}}function ensureSheet(){if(sheet&&sheet.isConnected)return sheet;sheet=document.createElement('div');sheet.className='agh-tow-sheet';sheet.hidden=true;sheet.innerHTML='<div class="agh-tow-panel" id="aghTowPanel"></div>';document.body.appendChild(sheet);sheet.onclick=e=>{if(e.target===sheet||e.target.closest('[data-tow-close]'))sheet.hidden=true};return sheet}function injectEntry(){if(isTow())return;const p=route();if(!['tournament','tournaments'].includes(p[0])||!p[1])return;if(document.getElementById('aghTowEntry'))return;const e=document.createElement('div');e.id='aghTowEntry';e.className='agh-tow-entry';e.innerHTML='<div><b>⭐ تشكيلة الأسبوع</b><span>كوّن أفضل 11 لاعبًا من هذه البطولة</span></div><button type="button">افتح التشكيلة</button>';e.querySelector('button').onclick=()=>location.hash=`#/team-of-week/${p[1]}`;main.prepend(e)}async function base(){if(S.tournaments.length)return;S.tournaments=await q('tournaments?select=id,name,division,logo_url,sort_order&order=sort_order');S.teams=await q('teams?select=id,name,logo_url,tournament_id')}function img(u,a){return u?`<img src="${esc(u)}" alt="${esc(a)}" loading="lazy">`:'👤'}function pitchHtml(readonly=false,source=S.slots){const lines=formations[S.formation]||formations['4-3-3'];return `<div class="agh-tow-pitch"><div class="agh-tow-center"></div><div class="agh-tow-lines">${lines.map(([g,keys])=>`<div class="agh-tow-line">${keys.map(k=>slotHtml(k,g,readonly,source)).join('')}</div>`).join('')}</div></div>`}function slotHtml(k,g,readonly,source){const id=source[k],p=id?player(id):null,t=p?team(p.team_id):null;return `<button type="button" class="agh-tow-slot ${p?'':'empty'}" ${readonly?'disabled':''} data-tow-slot="${k}" data-group="${g}"><span class="agh-tow-player-disc">${p?(p.photo_url?img(p.photo_url,p.player_name):'👤'):'+'}${p&&t?.logo_url?`<img class="agh-tow-team-logo" src="${esc(t.logo_url)}" alt="">`:''}</span><b>${p?esc(p.player_name):esc(g)}</b><small>${p?esc(t?.name||''):''}</small></button>`}function count(){return Object.values(S.slots).filter(Boolean).length}function draftKey(){return S.round?`agh_tow_draft_${S.round.id}`:''}function saveDraft(){if(!S.round)return;localStorage.setItem(draftKey(),JSON.stringify({formation:S.formation,slots:S.slots}))}function loadDraft(){if(!S.round)return;try{const d=JSON.parse(localStorage.getItem(draftKey())||'null');if(d&&d.formation&&d.slots){S.formation=d.formation;S.slots=d.slots}}catch{}}function isOpen(){if(!S.round||!S.round.is_open)return false;const n=Date.now();return(!S.round.opens_at||n>=new Date(S.round.opens_at).getTime())&&(!S.round.closes_at||n<new Date(S.round.closes_at).getTime())}function resultsHtml(){if(isOpen()&&!S.saved)return'';if(!S.stats.length)return'<div class="agh-tow-crowd"><h2>اختيارات الجمهور</h2><p class="agh-tow-note">لا توجد تشكيلات محفوظة بعد.</p></div>';const total=S.stats[0]?.participant_count||0;const top=[...S.stats].sort((a,b)=>Number(b.selection_percent)-Number(a.selection_percent))[0];const tp=player(top?.player_id);return `<div class="agh-tow-crowd"><h2>اختيارات الجمهور</h2><p class="agh-tow-note">👥 ${total} مشاركًا${top?` · 👑 الأكثر اختيارًا: ${esc(tp.player_name||'لاعب')} ${top.selection_percent}%`:''}</p>${S.stats.slice(0,12).map(s=>{const p=player(s.player_id);return `<div class="agh-tow-stat"><span>${esc(p.player_name||'لاعب')} · ${esc(s.position_group)}</span><b>${s.selection_percent}%</b><div class="agh-tow-statbar"><i style="width:${Math.min(100,Number(s.selection_percent)||0)}%"></i></div></div>`}).join('')}</div>`}function finalLineupHtml(){if(S.official.length){const src=Object.fromEntries(S.official.map(x=>[x.slot_key,x.player_id]));return `<div class="agh-tow-crowd"><span class="agh-tow-official-tag">✓ التشكيلة الرسمية</span><h2>⭐ تشكيلة الأسبوع</h2><p class="agh-tow-note">اعتماد إدارة كأس أغشوركيت</p>${pitchHtml(true,src)}</div>`}if(!isOpen()&&S.crowd.length>=11){const src=Object.fromEntries(S.crowd.map(x=>[x.slot_key,x.player_id]));return `<div class="agh-tow-crowd"><span class="agh-tow-official-tag">اختيار الجمهور</span><h2>⭐ تشكيلة الأسبوع</h2><p class="agh-tow-note">أفضل 11 لاعبًا حسب اختيارات الجمهور مع احترام المراكز</p>${pitchHtml(true,src)}</div>`}return''}function render(){if(!isTow())return;const tId=tournamentFromRoute()||S.tournaments[0]?.id;const t=S.tournaments.find(x=>x.id===tId)||S.tournaments[0];if(!t){main.innerHTML='<div class="agh-tow-page"><div class="agh-tow-empty">لا توجد بطولات.</div></div>';return}const rounds=S.rounds.filter(r=>r.tournament_id===t.id);if(!S.round||S.round.tournament_id!==t.id)S.round=rounds[0]||null;main.innerHTML=`<div class="agh-tow-page"><div class="agh-tow-hero"><div class="agh-tow-kicker">TEAM OF THE WEEK</div><h1>⭐ تشكيلة الأسبوع</h1><p>اختيار جمهور كأس أغشوركيت</p></div><div class="agh-tow-tabs">${S.tournaments.map(x=>`<button class="agh-tow-tab ${x.id===t.id?'active':''}" data-tow-tournament="${x.id}">${esc(x.division)}</button>`).join('')}</div>${rounds.length?`<div class="agh-tow-toolbar"><select class="agh-tow-select" data-tow-round>${rounds.map(r=>`<option value="${r.id}" ${S.round?.id===r.id?'selected':''}>${esc(r.label)}</option>`).join('')}</select><div class="agh-tow-status">${isOpen()?'🟢 الاختيار مفتوح':'🔒 مغلق'}</div></div><div class="agh-tow-toolbar"><select class="agh-tow-select" data-tow-formation ${S.saved&&!isOpen()?'disabled':''}>${['4-3-3','4-4-2','3-5-2'].map(f=>`<option ${S.formation===f?'selected':''}>${f}</option>`).join('')}</select><div class="agh-tow-status">${count()} / 11 لاعبًا</div></div>${pitchHtml(!isOpen())}<div class="agh-tow-progress"><span>${count()===11?'✓ التشكيلة مكتملة':'اختر 11 لاعبًا لإكمال تشكيلتك'}</span><strong>${count()} / 11</strong></div>${S.saved?'<div class="agh-tow-saved">✓ تم حفظ تشكيلتك لهذه الجولة</div>':''}<button class="agh-tow-submit" data-tow-submit ${!isOpen()||count()!==11?'disabled':''}>⚽ ${S.saved?'حفظ التعديلات':'إرسال تشكيلتي'}</button><div class="agh-tow-note">يمكنك التعديل حتى إغلاق باب الاختيار.</div>${resultsHtml()}${finalLineupHtml()}`:'<div class="agh-tow-empty">لم يتم فتح أي جولة لتشكيلة الأسبوع لهذه البطولة بعد.</div>'}</div>`;bind()}function bind(){main.querySelectorAll('[data-tow-tournament]').forEach(b=>b.onclick=async()=>{location.hash=`#/team-of-week/${b.dataset.towTournament}`});main.querySelector('[data-tow-round]')?.addEventListener('change',async e=>{S.round=S.rounds.find(r=>r.id===e.target.value)||null;S.saved=false;S.slots={};S.formation=S.round?.formation||'4-3-3';await loadRound();render()});main.querySelector('[data-tow-formation]')?.addEventListener('change',e=>{S.formation=e.target.value;const valid=new Set(formations[S.formation].flatMap(x=>x[1]));S.slots=Object.fromEntries(Object.entries(S.slots).filter(([k])=>valid.has(k)));saveDraft();render()});main.querySelectorAll('[data-tow-slot]').forEach(b=>b.onclick=()=>openPlayers(b.dataset.towSlot,b.dataset.group));main.querySelector('[data-tow-submit]')?.addEventListener('click',submit)}function openPlayers(slot,group){if(!isOpen())return;const sh=ensureSheet(),p=sh.querySelector('#aghTowPanel');const current=S.slots[slot];p.innerHTML=`<div class="agh-tow-panel-head"><div><h3>اختر ${esc(group)}</h3><p class="agh-tow-note">لا يمكن اختيار اللاعب نفسه مرتين.</p></div><button class="agh-tow-close" data-tow-close>×</button></div><div class="agh-tow-filters"><input class="agh-tow-search" data-tow-search placeholder="🔍 ابحث عن لاعب"><select class="agh-tow-search" data-tow-team><option value="">كل الفرق</option>${S.teams.filter(t=>S.players.some(x=>x.team_id===t.id)).map(t=>`<option value="${t.id}">${esc(t.name)}</option>`).join('')}</select></div><div class="agh-tow-player-list" data-tow-list></div>${current?'<button class="agh-tow-primary" data-tow-remove style="width:100%;margin-top:12px">إزالة اللاعب</button>':''}`;const list=p.querySelector('[data-tow-list]'),search=p.querySelector('[data-tow-search]'),tf=p.querySelector('[data-tow-team]');const draw=()=>{const term=search.value.trim().toLowerCase(),tid=tf.value,selected=new Set(Object.values(S.slots).filter(Boolean));let arr=S.players.filter(x=>(!term||x.player_name.toLowerCase().includes(term))&&(!tid||x.team_id===tid));const known=arr.filter(x=>x.player_position&&x.player_position!=='غير محدد');const hasKnown=known.some(x=>x.player_position===group);if(hasKnown)arr=arr.filter(x=>!x.player_position||x.player_position==='غير محدد'||x.player_position===group);if(group==='حارس مرمى'&&S.players.some(x=>x.player_position==='حارس مرمى'))arr=arr.filter(x=>x.player_position==='حارس مرمى');list.innerHTML=arr.map(x=>{const t=team(x.team_id),used=selected.has(x.player_id)&&x.player_id!==current;return `<button class="agh-tow-card" data-pick="${x.player_id}" ${used?'disabled':''}><span class="avatar">${x.photo_url?`<img class="avatar" src="${esc(x.photo_url)}" alt="">`:'👤'}</span><span><b>${esc(x.player_name)}</b><small>${esc(t.name||'')}${x.shirt_number!=null?` · #${x.shirt_number}`:''}${x.player_position?` · ${esc(x.player_position)}`:''}</small></span></button>`}).join('')||'<div class="agh-tow-empty">لا يوجد لاعبون مؤهلون لهذا المركز.</div>';list.querySelectorAll('[data-pick]').forEach(b=>b.onclick=()=>{S.slots[slot]=b.dataset.pick;S.saved=false;saveDraft();sh.hidden=true;render()})};search.oninput=draw;tf.onchange=draw;p.querySelector('[data-tow-remove]')?.addEventListener('click',()=>{delete S.slots[slot];S.saved=false;saveDraft();sh.hidden=true;render()});draw();sh.hidden=false}async function ensureIdentity(next){if(token())return next();const sh=ensureSheet(),p=sh.querySelector('#aghTowPanel');p.innerHTML='<div class="agh-tow-panel-head"><div><h3>تأكيد بيانات المشجع</h3><p class="agh-tow-note">نستخدم نفس هوية التوقعات ورجل المباراة لمنع التكرار.</p></div><button class="agh-tow-close" data-tow-close>×</button></div><form data-tow-register><input class="agh-tow-search" name="name" required minlength="2" placeholder="الاسم الظاهر"><input class="agh-tow-search" name="phone" required inputmode="tel" placeholder="رقم الهاتف" style="margin-top:8px"><button class="agh-tow-primary" style="width:100%;margin-top:10px">متابعة</button><div class="agh-tow-note" data-msg></div></form>';sh.hidden=false;p.querySelector('form').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),bt=e.currentTarget.querySelector('button'),msg=e.currentTarget.querySelector('[data-msg]');bt.disabled=true;bt.textContent='جارٍ التسجيل…';try{const r=await rpc('register_prediction_guest',{p_name:fd.get('name'),p_phone:fd.get('phone')});const u=r?.[0];if(!u?.session_token)throw 0;setToken(u.session_token);sh.hidden=true;await next()}catch{msg.textContent='تعذر تسجيل البيانات. حاول مرة أخرى.';bt.disabled=false;bt.textContent='متابعة'}}}async function submit(){if(count()!==11||!S.round)return;await ensureIdentity(async()=>{if(!confirm('هل تريد اعتماد هذه التشكيلة؟ يمكنك تعديلها لاحقًا قبل الإغلاق.'))return;const btn=main.querySelector('[data-tow-submit]');if(btn){btn.disabled=true;btn.textContent='جارٍ الحفظ…'}try{const selections=Object.entries(S.slots).map(([slot_key,player_id],i)=>({slot_key,player_id,position_group:slotGroup(slot_key),sort_order:i}));await rpc('save_weekly_lineup',{p_round_id:S.round.id,p_session_token:token(),p_formation:S.formation,p_selections:selections});S.saved=true;localStorage.removeItem(draftKey());await loadResults();render()}catch(e){alert(String(e.message).includes('ROUND_CLOSED')?'انتهى وقت اختيار تشكيلة الأسبوع.':'تعذر حفظ تشكيلتك. حاول مرة أخرى.');render()}})}async function loadResults(){if(!S.round)return;try{S.stats=await rpc('get_weekly_selection_stats',{p_round_id:S.round.id})||[];S.crowd=await rpc('get_weekly_crowd_lineup',{p_round_id:S.round.id})||[]}catch{S.stats=[];S.crowd=[]}try{S.official=await q(`weekly_lineup_official_players?round_id=eq.${S.round.id}&select=*`)}catch{S.official=[]}}async function loadRound(){if(!S.round)return;S.formation=S.round.formation||'4-3-3';S.players=await rpc('get_weekly_eligible_players',{p_round_id:S.round.id})||[];S.saved=false;S.slots={};if(token())try{const mine=await rpc('get_weekly_my_lineup',{p_round_id:S.round.id,p_session_token:token()})||[];if(mine.length){S.formation=mine[0].formation||S.formation;S.slots=Object.fromEntries(mine.map(x=>[x.slot_key,x.player_id]));S.saved=true}}catch{}if(!S.saved)loadDraft();await loadResults()}async function loadPage(){if(loading)return;loading=true;try{await base();S.rounds=await q('weekly_lineup_rounds?select=*&order=created_at.desc');const tid=tournamentFromRoute()||S.tournaments[0]?.id;const rounds=S.rounds.filter(r=>r.tournament_id===tid);S.round=rounds[0]||null;S.players=[];S.slots={};S.saved=false;if(S.round)await loadRound();render()}catch(e){main.innerHTML='<div class="agh-tow-page"><div class="agh-tow-error">تعذر تحميل تشكيلة الأسبوع. حاول مرة أخرى.</div></div>'}finally{loading=false}}let moTimer;new MutationObserver(()=>{clearTimeout(moTimer);moTimer=setTimeout(()=>{if(isTow())loadPage();else injectEntry()},180)}).observe(main,{childList:true,subtree:false});window.addEventListener('hashchange',()=>setTimeout(()=>{if(isTow())loadPage();else injectEntry()},220));window.addEventListener('load',()=>setTimeout(()=>{if(isTow())loadPage();else injectEntry()},900));setTimeout(()=>{if(isTow())loadPage();else injectEntry()},1400)})();