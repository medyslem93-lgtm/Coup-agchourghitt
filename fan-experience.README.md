# Fan experience

This release extends the existing public site without changing `public-app.js`, existing database tables, or admin workflows.

It adds device-local fan profiles, followed players, personalized sections, and a Watch hub powered by the site's existing matches, live stream fields, recaps, and match media.

The existing team favorites storage key is reused so prior favorites continue to work.
