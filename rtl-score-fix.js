(()=>{'use strict';function fix(root=document){root.querySelectorAll('.ref-match-card').forEach(card=>{const a=card.querySelector('.ref-team[data-side="a"] .ref-team-score');const b=card.querySelector('.ref-team[data-side="b"] .ref-team-score');const clock=card.querySelector('.ref-clock');if(!a||!b||!clock)return;clock.textContent=`${b.textContent.trim()} - ${a.textContent.trim()}`;clock.setAttribute('dir','ltr');clock.setAttribute('aria-label',`${a.textContent.trim()} للفريق على اليمين و${b.textContent.trim()} للفريق على اليسار`);});}function init(){const host=document.querySelector('#refMatchCards');if(!host)return;fix(host);new MutationObserver(()=>fix(host)).observe(host,{childList:true,subtree:true});}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();