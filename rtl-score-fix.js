(()=>{'use strict';
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const cfg=window.AGCH_CONFIG;const sb=(cfg&&window.supabase)?window.supabase.createClient(cfg.supabaseUrl,cfg.supabaseKey):null;
function go(page){$$('.page').forEach(x=>x.classList.toggle('active',x.id===page));$$('[data-page]').forEach(x=>x.classList.toggle('active',x.dataset.page===page));window.scrollTo(0,0);}
function fixCard(card){const a=card.querySelector('.ref-team[data-side="a"] .ref-team-score'),b=card.querySelector('.ref-team[data-side="b"] .ref-team-score'),clock=card.querySelector('.ref-clock');if(a&&b&&clock)clock.textContent=`${b.textContent.trim()} - ${a.textContent.trim()}`;}
function fixScores(root=document){root.querySelectorAll?.('.ref-match-card').forEach(fixCard);}
async function fixDetail(id){if(!sb||!id)return;const {data:m}=await sb.from('matches').select('score_a,score_b,status').eq('id',id).maybeSingle();if(!m||!['مباشر','جارية','انتهت'].includes(m.status))return;let tries=0;const apply=()=>{const score=document.querySelector('#sheet.show .match-detail .score');if(score){score.textContent=`${Number(m.score_b??0)} - ${Number(m.score_a??0)}`;score.setAttribute('dir','ltr');return}if(++tries<20)setTimeout(apply,50)};apply();}
function bind(){document.addEventListener('click',e=>{const match=e.target.closest('[data-open-match]');if(match)fixDetail(match.dataset.openMatch);const nav=e.target.closest('[data-page]');if(nav){e.preventDefault();e.stopPropagation();go(nav.dataset.page);return}const quick=e.target.closest('[data-quickcat]');if(quick){e.preventDefault();go('matches');const cat=quick.dataset.quickcat,btn=$(`[data-matchcat="${CSS.escape(cat)}"]`);if(btn)setTimeout(()=>btn.click(),0)}},true);window.go=go;const observer=new MutationObserver(ms=>{for(const m of ms)for(const n of m.addedNodes)if(n.nodeType===1)fixScores(n.matches?.('.ref-match-card')?n.parentElement:n)});observer.observe(document.body,{childList:true,subtree:true});fixScores();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();