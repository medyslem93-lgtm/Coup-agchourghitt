(()=>{'use strict';const cfg=window.AGCH_CONFIG||{};if(!cfg.supabaseUrl||!cfg.supabaseKey)return;const H={apikey:cfg.supabaseKey,Authorization:`Bearer ${cfg.supabaseKey}`};const q=async p=>{const r=await fetch(`${cfg.supabaseUrl}/rest/v1/${p}`,{headers:H,cache:'no-store'});if(!r.ok)throw new Error(`${p}:${r.status}`);return r.json()};const esc=(v='')=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const norm=(v='')=>String(v).replace(/\s+/g,' ').trim();let layer;function ensureLayer(){if(layer&&layer.isConnected)return layer;layer=document.createElement('div');layer.className='agh-standings-detail';layer.hidden=true;layer.innerHTML='<div class="agh-standings-detail-panel"></div>';document.body.appendChild(layer);layer.onclick=e=>{if(e.target===layer||e.target.closest('[data-close-standings]'))layer.hidden=true};return layer}const gd=v=>{const n=Number(v)||0;return `${n>0?'+':''}${n}`};async function render(){const old=document.querySelector('.standings-table');if(!old)return;const host=old.closest('.table-wrap')||old.parentElement;if(!host||host.dataset.fairplayV9==='1')return;let all=[];try{all=await q('tournament_standings?select=*&order=tournament_id.asc,group_name.asc,position.asc')}catch(e){console.error('fairplay standings',e);return}const visibleNames=[...old.querySelectorAll('.standings-team b')].map(x=>norm(x.textContent)).filter(Boolean);if(!visibleNames.length)return;const score=new Map();all.forEach(r=>{const hit=visibleNames.includes(norm(r.team_name));score.set(r.tournament_id,(score.get(r.tournament_id)||0)+(hit?1:0))});const tid=[...score.entries()].sort((a,b)=>b[1]-a[1])[0]?.[0];if(!tid||!score.get(tid))return;const rows=all.filter(r=>r.tournament_id===tid);const currentGroupTitle=old.closest('.standings-card,.section-block')?.querySelector('.group-title')?.textContent||'';let groups=[...new Set(rows.map(r=>r.group_name||'الترتيب العام'))];if(visibleNames.length<rows.length){groups=groups.filter(g=>rows.filter(r=>(r.group_name||'الترتيب العام')===g).some(r=>visibleNames.includes(norm(r.team_name))))}host.dataset.fairplayV9='1';host.innerHTML=`<div class="agh-standings-v9">${groups.map(g=>{const gr=rows.filter(r=>(r.group_name||'الترتيب العام')===g);return `<section class="agh-standings-group"><div class="agh-standings-group-head"><div><small>GROUP STANDINGS</small><h3>${g==='الترتيب العام'?g:`المجموعة ${esc(g)}`}</h3></div><small>PT ← GD ← GF ← اللعب النظيف</small></div><div class="agh-standings-scroll"><table class="agh-standings-table"><thead><tr><th class="st-col">ST</th><th class="team-col">الفريق</th><th>P</th><th>GD</th><th class="pt">PT</th><th>W</th><th>D</th><th>L</th><th>GF</th><th>GA</th><th>YC 🟨</th><th>RC 🟥</th><th>FP</th></tr></thead><tbody>${gr.map((r,i)=>`<tr class="${i<2?'qualify':''}" data-standings-team="${r.team_id}"><td class="st-col">${r.position}</td><td class="team-col"><span class="agh-standings-team">${r.logo_url?`<img src="${esc(r.logo_url)}" alt="">`:''}<b>${esc(r.team_name)}</b></span></td><td>${r.played}</td><td class="gd ${Number(r.goal_difference)>0?'plus':''}">${gd(r.goal_difference)}</td><td class="pt">${r.points}</td><td>${r.won}</td><td>${r.drawn}</td><td>${r.lost}</td><td>${r.goals_for}</td><td>${r.goals_against}</td><td class="agh-card-count">${r.yellow_cards||0}</td><td class="agh-card-count">${r.red_cards||0}</td><td class="agh-fp">${r.fair_play_points||0}</td></tr>`).join('')}</tbody></table></div><div class="agh-standings-note"><span>🟨 بطاقة صفراء</span><span>🟥 بطاقة حمراء</span><span>FP الأقل أفضل عند استمرار التعادل</span></div></section>`}).join('')}</div>`;host.querySelectorAll('[data-standings-team]').forEach(tr=>tr.onclick=()=>detail(tr.dataset.standingsTeam,tid,rows.find(x=>x.team_id===tr.dataset.standingsTeam)))}async function detail(teamId,tid,row){const l=ensureLayer(),p=l.querySelector('.agh-standings-detail-panel');let events=[];try{events=await q(`match_events?team_id=eq.${teamId}&type=in.(بطاقة صفراء,بطاقة حمراء)&select=id,type,minute,player_name,player_id,match_id,created_at&order=created_at.desc`)}catch{}p.innerHTML=`<div class="agh-standings-detail-head"><div class="agh-standings-detail-team">${row.logo_url?`<img src="${esc(row.logo_url)}" alt="">`:''}<div><small>تفاصيل الترتيب</small><h3>${esc(row.team_name)}</h3></div></div><button class="agh-standings-detail-close" data-close-standings>×</button></div><div class="agh-standings-detail-grid">${[['لعب',row.played],['فاز',row.won],['تعادل',row.drawn],['خسر',row.lost],['له',row.goals_for],['عليه',row.goals_against],['فارق',gd(row.goal_difference)],['نقاط',row.points],['🟨',row.yellow_cards||0],['🟥',row.red_cards||0],['اللعب النظيف',row.fair_play_points||0]].map(([a,b])=>`<div><b>${b}</b><span>${a}</span></div>`).join('')}</div><h4>البطاقات المسجلة</h4><div class="agh-card-events">${events.length?events.map(e=>`<div class="agh-card-event"><div><b>${e.type==='بطاقة حمراء'?'🟥':'🟨'} ${esc(e.player_name||'لاعب غير محدد')}</b><small>${e.minute!=null?`الدقيقة ${e.minute}`:'الدقيقة غير مسجلة'}</small></div></div>`).join(''):'<div class="agh-card-event"><div><b>لا توجد بطاقات مسجلة</b><small>0 بطاقات</small></div></div>'}</div>`;l.hidden=false}let t;const kick=()=>{clearTimeout(t);t=setTimeout(render,250)};new MutationObserver(kick).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('hashchange',()=>setTimeout(render,500));window.addEventListener('load',()=>setTimeout(render,700));setTimeout(render,1000)})();